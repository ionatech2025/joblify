// Same page-body skeleton as the parent group. Having it at this level keeps
// the real jobseeker sub-nav on screen during tab-to-tab navigations — only
// the page body below it swaps to the skeleton.
export { default } from '../loading';
